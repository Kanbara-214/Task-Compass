# Optional: copy this file to config/application.properties to use PostgreSQL instead of the default H2 database.
spring.datasource.url=jdbc:postgresql://localhost:5432/task_compass
spring.datasource.username=postgres
spring.datasource.password=change-me
